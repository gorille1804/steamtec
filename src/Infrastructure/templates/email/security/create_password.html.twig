<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{'users.create_passwords.title'|trans}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
        }
        .container {
            max-width: 600px;
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #333;
        }
        p {
            color: #555;
        }
        .btn {
            display: inline-block;
            background: #007bff;
            color: white;
            text-decoration: none;
            padding: 10px 15px;
            border-radius: 5px;
            margin-top: 20px;
        }
        .footer {
            margin-top: 20px;
            font-size: 12px;
            color: #777;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{'users.create_passwords.title'|trans}}</h1>
        <p>{{'users.create_passwords.hello'|trans}} {{ user.email }},</p>
        <p>{{'users.create_passwords.text'|trans}}</p>
        <p>
            <a href="{{ resetLink }}" class="btn">{{'users.create_passwords.link'|trans}}</a>
        </p>
        <p>{{'users.create_passwords.duration'|trans}}</p>
        <p>{{'users.create_passwords.info'|trans}}</p>
        <div class="footer">
            <p>{{'users.create_passwords.thanks'|trans}}</p>
            <p>{{'users.create_passwords.reaction'|trans}}</p>
        </div>
    </div>
</body>
</html>