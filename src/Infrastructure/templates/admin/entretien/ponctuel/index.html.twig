{% extends 'layout/dashboard.layout.html.twig' %}
{% block title %}Entretien ponctuel machine{% endblock %}

{% block content %}
    <style>
        .alert-animated {
            animation: pulse 1s infinite;
        }
        
        @keyframes pulse {
            0% { 
                transform: scale(1);
                box-shadow: 0 0 0 0 rgba(255, 193, 7, 0.7);
            }
            50% { 
                transform: scale(0.98);
                box-shadow: 0 0 0 10px rgba(255, 193, 7, 0);
            }
            100% { 
                transform: scale(1);
                box-shadow: 0 0 0 1px rgba(255, 193, 7, 0);
            }
        }
    </style>

    <div class="row flex-grow">
        <div class="col-md-12">
            {% for message in app.flashes('success') %}
                <div class="alert alert-success">
                    {{ message }}
                </div>
            {% endfor %}
        </div>
        <div class="col-12 grid-margin stretch-card">
            <div class="card card-rounded">
                <div class="card-body">
                    <div class="d-sm-flex justify-content-between align-items-start">
                        <div>
                            <h2 class="card-title card-title-dash">Entretien ponctuel machine</h2>
                        </div>
                        <div>
                            <a href="#" class="btn btn-primary btn-md text-white mb-0 me-0" type="button">
                                <i class="mdi mdi-plus"></i> Ajouter un entretien ponctuel
                            </a>
                        </div>
                    </div>
                    
                    <div class="alert alert-warning mt-3 alert-animated" role="alert">
                        <i class="mdi mdi-alert-circle"></i>
                        <strong>Attention :</strong> Pour toute activité d'entretien ou de dépannage : machine à l'arrêt et hors tension
                    </div>
                    
                    <div class="mt-3 text-center">
                        <p>Page d'entretien ponctuel machine en cours de développement.</p>
                        <p>Cette page permettra de gérer les entretiens ponctuels et interventions spécifiques sur les machines.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %} 